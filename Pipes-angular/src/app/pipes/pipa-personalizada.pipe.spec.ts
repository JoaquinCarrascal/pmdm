import { PipaPersonalizadaPipe } from './pipa-personalizada.pipe';

describe('PipaPersonalizadaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipaPersonalizadaPipe();
    expect(pipe).toBeTruthy();
  });
});
